<div class="container">
  <h1>Favorites' lists </h1>
  <div class="row">
    <div class="cards mb-5">
      <% @lists.each do |list| %>
        <%= render "list_card", list: list %>
      <% end %>
    </div>
  </div>
  <div class="row">
    <%= render "list_form" %>
  </div>
</div>
